{
  "name": "typescript-node-scaffold",
  "version": "0.1.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "node dist/index.js",
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "typecheck:watch": "tsc --watch --noEmit",
    "clean": "rm -rf dist",
    "test": "jest",
    "format": "prettier --write .",
    "prepare-husky": "husky install",
    "noop": "echo Nothing happened. This line is just here so the real lines can all have commas."
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@babel/core": "7.16.0",
    "@babel/preset-env": "7.16.4",
    "@babel/preset-typescript": "7.16.0",
    "babel-jest": "27.3.1",
    "husky": "7.0.4",
    "jest": "27.3.1",
    "lint-staged": "12.0.3",
    "prettier": "2.4.1",
    "typescript": "4.5.2"
  },
  "lint-staged": {
    "**/*": "prettier --write --ignore-unknown"
  }
}
